task distZip(type: Zip) {
   from('PE-Rebrander/Release') {
       include 'PE-Rebrander.exe'
   }
   from('.') {
       include 'README.md'
       include 'LICENSE'
   }
   archiveName 'PE-rebrander.zip'
   destinationDir(file('build'))
}
